{% for group in groups %}
{% if group=='all' %}
{% elif group=='ungrouped' %}
{% elif group=='aws_ec2' %}
{% else %}
[{{ group }}]
{% for host in groups[group] %}
{{ host }} ansible_ssh_user={{ hostvars[host]['ansible_ssh_user']}} ansible_ssh_private_key_file={{ hostvars[host]['ansible_ssh_private_key_file'] }}
{% endfor %}
{% endif %}
{% endfor %}